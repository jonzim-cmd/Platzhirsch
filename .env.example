# Prisma uses this connection string for Vercel Postgres
# Replace with your actual connection string in .env
DATABASE_URL="postgresql://user:password@pooler-host/db?sslmode=require&pgbouncer=true&connection_limit=1&connect_timeout=5"

{
  "buildCommand": "npx prisma generate && npx prisma migrate resolve --rolled-back 20250929164834_init || true && npx prisma migrate deploy && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "devCommand": "npm run dev"
}
